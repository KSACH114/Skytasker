<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Activity 窗口主题。真正的配色由 Compose 的 MaterialTheme 决定（ui/theme/Theme.kt），
      这里只需要一个不带 ActionBar 的容器主题（界面顶栏由 Compose 自己画）。
      状态栏交给 enableEdgeToEdge() 处理，所以不再设置 statusBarColor。
    -->
    <style name="Theme.Skytasker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>
</resources>
